# Promotion Chain - การปรับปรุง Logic การเลือกผู้สมัคร

## 📋 สรุปการเปลี่ยนแปลง

### ✅ ปรับปรุงแล้ว:

#### **1. ไม่จำกัด PosCode ของผู้สมัคร**

**เดิม:** ระบบจะหาผู้สมัครที่มี `rankLevel = targetRankLevel + 1` เท่านั้น

**ใหม่:** ระบบจะหาผู้สมัครที่มี `rankLevel > targetRankLevel` ทั้งหมด

**ตัวอย่าง:**
- ตำแหน่งว่าง: **ผกก.** (Level 8)
- ผู้สมัครที่แสดง:
  - ✅ **รอง ผกก.** (Level 9) - เลื่อนขึ้น 1 ระดับ
  - ✅ **สว.** (Level 11) - เลื่อนขึ้น 3 ระดับ
  - ✅ **รอง สว.** (Level 12) - เลื่อนขึ้น 4 ระดับ

#### **2. เงื่อนไขการเลื่อนตำแหน่ง**

- ✅ **ต้องเป็นการเลื่อนขึ้น:** `toRankLevel < fromRankLevel`
- ✅ **จาก Level 8 ไม่สามารถไปมากกว่า 8 ได้:** คนที่ Level 8 สามารถเลื่อนไปเป็น Level 6 หรือ 7 ได้ แต่ไม่สามารถไป Level 9 ขึ้นไป
- ✅ **คนที่ Level สูงสามารถเลื่อนข้าม Level ได้:** เช่น Level 11 สามารถเลื่อนไปเป็น Level 8 ได้ทันที

## 🔍 ตัวอย่างการใช้งาน

### **Scenario 1: ตำแหน่งว่าง ผกก. (Level 8)**

```
ผกก-นครปฐม (Level 8) ว่าง
```

**ผู้สมัครที่แสดง:**
```
✅ พ.ต.ต. สมปอง วีรชน - รอง ผกก.-เพชรบุรี (Level 9)
✅ พ.ต.ต. สมคิด แกล้วกล้า - รอง ผกก.-ประจวบฯ (Level 9)
✅ พ.ต.ต. สมใจ ซื่อสัตย์ - สว.-กาญจนบุรี (Level 11)
✅ พ.ต.ต. สมบูรณ์ ยุติธรรม - สว.-เพชรบุรี (Level 11)
✅ ร.ต.อ. สมศักดิ์ กล้าหาญ - รอง สว.-สุพรรณบุรี (Level 12)
✅ ร.ต.อ. สมนึก อุทิศ - รอง สว.-ราชบุรี (Level 12)
```

### **Scenario 2: เลือก สว. (Level 11) มาเป็น ผกก. (Level 8)**

```
ขั้นที่ 1:
พ.ต.ต. สมใจ ซื่อสัตย์ (สว.-กาญจนบุรี, Level 11)
→ เลื่อนขึ้นเป็น ผกก-นครปฐม (Level 8) ✅

ผลลัพธ์:
- สว.-กาญจนบุรี (Level 11) ว่าง
```

**ขั้นถัดไป - ผู้สมัครที่แสดง:**
```
✅ ร.ต.อ. สมศักดิ์ กล้าหาญ - รอง สว.-สุพรรณบุรี (Level 12)
✅ ร.ต.อ. สมนึก อุทิศ - รอง สว.-ราชบุรี (Level 12)
```

### **Scenario 3: Chain ที่สมบูรณ์**

```
ผกก-นครปฐม (Level 8) ว่าง
    ↓
พ.ต.ต. สมใจ ซื่อสัตย์ (สว.-กาญจนบุรี, Level 11)
→ เลื่อนเป็น ผกก-นครปฐม (Level 8)
    ↓ สว.-กาญจนบุรี (Level 11) ว่าง
ร.ต.อ. สมศักดิ์ กล้าหาญ (รอง สว.-สุพรรณบุรี, Level 12)
→ เลื่อนเป็น สว.-กาญจนบุรี (Level 11)
    ✓ จบ Chain
```

## 🎯 Code Changes

### **1. PromotionChainBuilder.tsx**

**เดิม:**
```typescript
const getNextRequiredRankLevel = (): number | null => {
  if (nodes.length === 0) {
    return vacantRankLevel + 1; // หา Level ถัดไปเท่านั้น
  }
  return lastNode.fromRankLevel + 1;
};
```

**ใหม่:**
```typescript
const getCurrentVacantRankLevel = (): number | null => {
  if (nodes.length === 0) {
    return getRankLevelByPosCode(vacantPosition?.posCodeId || 0); // ระดับของตำแหน่งว่าง
  }
  return lastNode.fromRankLevel; // ระดับของตำแหน่งที่เพิ่งว่าง
};
```

### **2. CandidateSelector.tsx**

**เดิม:**
```typescript
// แสดงเฉพาะคนที่มี rankLevel = requiredRankLevel
const mockData = allMockData[requiredRankLevel] || [];
```

**ใหม่:**
```typescript
// แสดงทุกคนที่มี rankLevel > targetRankLevel (ยศต่ำกว่า)
const filteredData = allMockData.filter(p => p.rankLevel > targetRankLevel);
```

**Props เปลี่ยนจาก:**
```typescript
interface CandidateSelectorProps {
  requiredRankLevel: number; // ระดับที่ต้องการ (เดิม)
}
```

**เป็น:**
```typescript
interface CandidateSelectorProps {
  targetRankLevel: number; // ระดับของตำแหน่งว่าง (ใหม่)
}
```

## 📊 Rank Hierarchy

```
Level 1  → รอง ผบ.ตร. (สูงสุด)
Level 2  → ผู้ช่วย
Level 3  → ผบช.
Level 4  → รอง ผบช.
Level 6  → ผบก.
Level 7  → รอง ผบก.
Level 8  → ผกก.
Level 9  → รอง ผกก.
Level 11 → สว.
Level 12 → รอง สว. (ต่ำสุด)
```

**หมายเหตุ:** ตัวเลขน้อย = ยศสูง, ตัวเลขมาก = ยศต่ำ

## ✅ Validation Rules

### **1. การเลื่อนตำแหน่งต้องถูกต้อง**
```typescript
isPromotionValid = toRankLevel < fromRankLevel
```

**ตัวอย่าง:**
- Level 11 (สว.) → Level 8 (ผกก.) = `8 < 11` ✅ ถูกต้อง
- Level 8 (ผกก.) → Level 11 (สว.) = `11 < 8` ❌ ไม่ถูกต้อง

### **2. Filter ผู้สมัคร**
```typescript
candidates = allCandidates.filter(p => p.rankLevel > targetRankLevel)
```

**ตัวอย่าง:**
- ตำแหน่งว่าง: Level 8
- ผู้สมัคร: Level 9, 11, 12 (ทั้งหมดที่ > 8)

## 🎨 UI Changes

### **1. Info Alert**
```
ตำแหน่งที่ว่าง: ผกก-นครปฐม (สถ.นครปฐม)
ระดับตำแหน่ง: ผกก.
หาผู้สมัคร: ที่มียศต่ำกว่า (Level > 8) เพื่อเลื่อนขึ้นมา
```

### **2. Add Node Button**
```
ตำแหน่งที่ว่าง: ผกก.-สมุทรสาคร
ต้องการ: ยศที่ต่ำกว่า ผกก. (สามารถเลื่อนขึ้นได้)
[เลือกผู้สมัคร]
```

## 🧪 Test Cases

### **Test 1: เลือก Level 9 มาเป็น Level 8**
```
Input:
- Vacant: ผกก. (Level 8)
- Select: รอง ผกก. (Level 9)

Expected:
✅ สามารถเลือกได้
✅ isPromotionValid = true (8 < 9)
✅ ตำแหน่งใหม่ว่าง: รอง ผกก. (Level 9)
```

### **Test 2: เลือก Level 11 มาเป็น Level 8**
```
Input:
- Vacant: ผกก. (Level 8)
- Select: สว. (Level 11)

Expected:
✅ สามารถเลือกได้
✅ isPromotionValid = true (8 < 11)
✅ ตำแหน่งใหม่ว่าง: สว. (Level 11)
✅ เลื่อนข้าม 3 Level (8, 9, 10, 11)
```

### **Test 3: เลือก Level 7 มาเป็น Level 8**
```
Input:
- Vacant: ผกก. (Level 8)
- Select: รอง ผบก. (Level 7)

Expected:
❌ ไม่แสดงในรายการ
❌ เพราะ Level 7 < Level 8 (ยศสูงกว่า ไม่สามารถเลื่อนมาได้)
```

## 📝 สรุป

### ความแตกต่างหลัก:

| | เดิม | ใหม่ |
|---|---|---|
| **Filter ผู้สมัคร** | `rankLevel = target + 1` | `rankLevel > target` |
| **จำนวนผู้สมัคร** | จำกัดเฉพาะ 1 ระดับ | แสดงทุกระดับที่ต่ำกว่า |
| **การเลื่อนข้าม Level** | ❌ ไม่ได้ | ✅ ได้ |
| **ความยืดหยุ่น** | ต่ำ | สูง |

### ข้อดี:
- ✅ ยืดหยุ่นในการเลือกผู้สมัคร
- ✅ สามารถเลื่อนข้าม Level ได้ (เช่น Level 11 → 8)
- ✅ ใกล้เคียงกับความเป็นจริงมากขึ้น
- ✅ ลด Chain ที่ยาวเกินไป

### การทดสอบ:
```bash
npm run dev
# เข้า http://localhost:3000/police-personnel/promotion-chain
# คลิก "สร้าง Chain ใหม่"
# เลือกตำแหน่งว่าง "ผกก-นครปฐม"
# จะเห็นผู้สมัครทุก Level ที่ > 8 (9, 11, 12)
```

สำเร็จ! 🎉
